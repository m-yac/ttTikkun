[tool.poetry]
name = "combine"
version = "0.1.0"
description = ""
authors = ["Matthew Yacavone <matthew@yacavone.net>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
fonttools = "^4.60.1"
requests = "^2.32.5"
lxml = "^6.0.2"
num2words = "^0.5.14"

[tool.poetry.scripts]
combine = "combine.main:main"
validate-ult = "combine.usfm:main"
pull-clean-ult = "combine.pull_clean_ult:main"


[tool.poetry.group.dev.dependencies]
pytest = "^9.1.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
