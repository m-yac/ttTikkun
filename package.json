{
  "name": "ttTikkun",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "relock": "rm -rf node_modules package-lock.json && npm install --include=optional"
  },
  "dependencies": {
    "havarotjs": "github:m-yac/havarotjs#merged-better-syllable-structure"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.0.0"
  }
}
